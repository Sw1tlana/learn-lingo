{
  "rules": {
    "users": {
      "$user_id": {
        ".read": "$user_id === auth.uid", // Тільки авторизований користувач може читати свої дані
        ".write": "$user_id === auth.uid" // Тільки авторизований користувач може записувати свої дані
      }
    }
  }
}